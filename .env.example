# Optional for local development. Without DATABASE_URL, the app uses safe in-memory/fallback data.
DATABASE_URL=mysql://USER:PASSWORD@HOST:3306/DATABASE
JWT_SECRET=replace-with-a-long-random-secret
OWNER_OPEN_ID=
OWNER_NAME=Mary

# Only required if Manus OAuth/storage features are kept enabled.
VITE_APP_ID=
OAUTH_SERVER_URL=
VITE_OAUTH_PORTAL_URL=
BUILT_IN_FORGE_API_URL=
BUILT_IN_FORGE_API_KEY=
